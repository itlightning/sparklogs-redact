# Changelog

All notable changes to `@sparklogs/redact-cli` are documented here. This project adheres to
[Semantic Versioning](https://semver.org/).

Package versions in this monorepo are released in **lockstep** with `@sparklogs/redact-core` and
`@sparklogs/redact-react` (same version number; see repo root README).

## Unreleased

## 0.1.0

Initial public release.

- Node CLI (`sparklogs-redact`): `redact`, `scan`, and `profiles` commands.
- Bundles `@sparklogs/redact-core` into a self-contained `dist/cli.js`.
